dist/
node_modules/
# One-shot claim sentinel (scripts/claim-simplefin-token.ts). Contains only a
# timestamp + host, never the access URL, but it is machine-local state.
.claimed
# StrykerJS mutation-testing artifacts (npm run test:mutation).
# reports/ holds the html/json reports AND the incremental-mode cache
# (reports/stryker-incremental.json) - machine-local, never committed.
.stryker-tmp/
reports/
stryker.log
