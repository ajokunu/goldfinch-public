{
  "name": "@goldfinch/feature-investments",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GoldFinch investments feature: the per-account detail screen with the holdings positions table (P7-3) AND the top-level Investments tab that aggregates holdings across every investment account (symbol-grouped, per-currency totals). Lives inside the @goldfinch/app Expo workspace; not an independent npm workspace.",
  "main": "./index.tsx",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "clean": "rm -rf dist-test"
  },
  "dependencies": {
    "@goldfinch/shared": "*",
    "@tanstack/react-query": "^5.59.16",
    "expo-router": "~6.0.0",
    "lucide-react-native": "^0.511.0",
    "react": "19.1.0",
    "react-native": "0.81.4"
  }
}
