{
  "name": "@goldfinch/feature-investments",
  "version": "0.1.0",
  "private": true,
  "description": "GoldFinch investments feature (P7-3): account detail screen with the holdings positions table (symbol/shares/value/cost basis) and the explicit no-holdings-from-institution state. Lives inside the @goldfinch/app Expo workspace; not an independent npm workspace.",
  "main": "./index.tsx",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@goldfinch/shared": "*",
    "@tanstack/react-query": "^5.59.16",
    "expo-router": "~6.0.0",
    "lucide-react-native": "^0.511.0",
    "react": "19.1.0",
    "react-native": "0.81.4"
  }
}
