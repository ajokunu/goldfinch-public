{
  "name": "@goldfinch/feature-budget",
  "version": "0.1.0",
  "private": true,
  "description": "GoldFinch budgeting feature (master plan section 15): category management, envelope budgets with rollover, per-category progress from GSI2-backed actuals, monthly cash flow, recategorize flow. Lives inside the @goldfinch/app Expo workspace; not an independent npm workspace.",
  "main": "./index.tsx",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "clean": "rm -rf dist-test"
  },
  "dependencies": {
    "@goldfinch/shared": "*",
    "@tanstack/react-query": "^5.59.16",
    "lucide-react-native": "^0.469.0",
    "react": "19.1.0",
    "react-native": "0.81.4"
  }
}
