/**
 * Thin re-export of the promoted shared segmented control
 * (app/src/ui/Segmented.tsx, components.md 4.4). Identical
 * options/value/onChange contract; the kit version adds the theme-driven
 * active treatment (raised surface vs accent fill) and select-motion
 * crossfade.
 */
export { SegmentedTabs } from '../../../src/ui/Segmented';
export type { SegmentedTabsProps } from '../../../src/ui/Segmented';
