{
  "name": "@goldfinch/dashboard-feature",
  "version": "0.1.0",
  "private": true,
  "description": "GoldFinch dashboard feature (net worth, accounts, recent activity). Lives inside the @goldfinch/app workspace; dependencies are owned by app/package.json.",
  "main": "index.tsx",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/features/dashboard/test/*.test.js",
    "clean": "rm -rf dist-test"
  }
}
