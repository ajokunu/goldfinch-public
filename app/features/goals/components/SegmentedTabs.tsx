/**
 * Thin re-export of the promoted shared segmented control
 * (app/src/ui/Segmented.tsx). Identical option/value/onChange contract to the
 * historical feature-local copy.
 */
export {
  SegmentedTabs,
  type SegmentedTabsProps,
} from '../../../src/ui/Segmented';
