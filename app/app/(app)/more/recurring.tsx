/**
 * Recurring route (P7-1). Thin typed route binding; the screen body is owned
 * by the recurring feature part (features/recurring/).
 */
import RecurringScreen from '../../../features/recurring';

export default RecurringScreen;
