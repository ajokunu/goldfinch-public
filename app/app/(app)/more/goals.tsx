/**
 * Goals route (P7-2). Thin typed route binding; the screen body is owned by
 * the goals feature part (features/goals/).
 */
import GoalsScreen from '../../../features/goals';

export default GoalsScreen;
