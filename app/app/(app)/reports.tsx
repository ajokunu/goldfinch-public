/**
 * Reports tab route (P7-4). The screen body is owned by the reports feature
 * part (features/reports/); this file stays a thin typed route binding.
 */
import ReportsScreen from '../../features/reports';

export default ReportsScreen;
