/**
 * Dashboard tab route. The screen body is owned by the dashboard feature part
 * (features/dashboard/); this file stays a thin typed route binding.
 */
import DashboardScreen from '../../features/dashboard';

export default DashboardScreen;
