/**
 * Budget tab route. The screen body is owned by the budgeting feature part
 * (features/budget/); this file stays a thin typed route binding.
 */
import BudgetScreen from '../../features/budget';

export default BudgetScreen;
